# macOS
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# uv / virtualenv
.venv/
venv/
ENV/
env/
.uv-cache/
.python-eggs/

# Test / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
coverage.xml
*.cover

# Type checkers
.mypy_cache/
.pyright/
.ruff_cache/

# IDE / agent local state
.vscode/
.idea/
.claude/
*.swp
*.swo
*~
.ipynb_checkpoints/

# Env / secrets — never commit
.env
.env.*
!.env.example
.envrc
.direnv/
*.pem
*.key
# Doppler CLI auth state（doppler.yaml 是仓库内的项目声明，*应该* commit；
# .doppler/ 是用户级 auth state，绝不 commit）
.doppler/

# OpenBot runtime / artifacts
.openbot/artifacts/
artifacts/
*.log
*.sqlite
*.sqlite3
*.db
# Local eval / agent runs — all output unified under evals/results/
eval-logs/
inspect-logs/
# inspect-ai default log dir; PRD §8.4 forbids commit
logs/
# any stray eval log file
*.eval
.inspect/
.langgraph/
# Docker
docker-compose.override.yml

# Memory / scratch — local-only
.remember/
.scratch/

# Large benchmark datasets — committed in gzipped form; the plain JSONL is
# lazily materialized at task load time (evals/tasks/review_martian.py).
evals/datasets/martian_2026w20.jsonl
.smoke/
.worktrees/
