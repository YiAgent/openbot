# Handoff · E1-T09 · Run summary 导出脚本

**Date**: 2026-05-15
**Branch**: eval/E1-T09 (Graphite stack on eval/E1-T08)
**Commits**: _(this commit)_
**Status**: ❌ blocked
**Task spec**: [`task-list.md` → `### E1-T09`](../task-list.md)
**PRD ref**: eval PRD §11.3 · §14 E1 验收

## Blocker
Deps:
- **E1-T08** (Martian smoke task) — ❌ blocked → 没真 run 可 summarize
- 实现还间接需要 **E1-T04** unblock 才能从 LangSmith 拉数据。

## What was produced (partial)
- `evals/scripts/export_run_summary.py` — `main(argv)` 占位：直接 print stderr + exit 2，指向本 handoff。

## AC checklist
- [ ] 输出含：suite 名 / 总 sample / 通过率 / 总 cost / failure_category 分布 / 异常 top-5 — **blocked: E1-T08 + E1-T04**

## Unblock checklist
1. E1-T08 + E1-T04 都 ✅。
2. 实现 `main(argv)`：
   - `argparse` 接受 `<run_id> [--out PATH]`。
   - 用 `evals.common.langsmith.init_client(public=False)` 拿 client。
   - `client.list_runs(filter=run_id ⋯)` 拉 sample-level 数据。
   - 聚合 6 个指标（suite name from run metadata、total sample、pass rate、total cost_usd、`failure_category` Counter、top-5 by cost）。
   - 输出 markdown 表 + 一行总结，写入 `--out` 或 stdout。
3. 跑一次产 `docs/reports/eval-sample-summary.md` 示例输出（用 E1-T08 跑出来的真 run）。
4. handoff 改为 `✅ done`，STATE.md 移到 Done，commit `feat(E1-T09): ...`。

## Tests run
```
$ make check  # 124 passed in <=2s (no new tests; stub only)
```

## Pointers for next task
- **E1-T10** · 双 LangSmith project 隔离 —— Deps E1-T02 ✅，**本 stack 可以做**。E1 stack 最后一个有产出 task。
- E1 全部完成后，Graphite stack 整体 submit。

## Open follow-ups
- 见 unblock checklist。
