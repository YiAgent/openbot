# Handoff · E1-T08 · Martian 最小 task（3-5 sample 验通）

**Date**: 2026-05-15
**Branch**: eval/E1-T08 (Graphite stack on eval/E1-T07)
**Commits**: _(this commit)_
**Status**: ❌ blocked
**Task spec**: [`task-list.md` → `### E1-T08`](../task-list.md)
**PRD ref**: eval PRD §4.1 · §14 E1 验收

## Blocker

Deps:
- **E1-T06** (review solver) — ❌ blocked: `openbot.workflows.review` 不存在
- **E1-T07** (review overlap scorer) — ✅ done（本 stack）
- **E0-T04** (validator script) — ✅ done

E1-T08 的 AC「跑完每 sample 有 trace / score / cost / artifact；LangSmith run metadata 校验脚本通过」要求一个端到端 run，需要 E1-T06 + E1-T04 都解开后才能落地。

## What was produced (partial)

- `evals/tasks/review_martian.py` — `martian_review_task()` raises NotImplementedError 占位；docstring 写清楚 unblock 后的接线步骤。

## AC checklist
- [ ] 跑完每 sample 有 trace / score / cost / artifact — **blocked: E1-T06 + E1-T04**
- [ ] LangSmith run metadata 校验脚本通过 — **blocked: 同上（无 run 可校验）**

## Unblock checklist

When E1-T06 and E1-T04 are both ✅:

1. 实现 `martian_review_task()`：
   - `@task` from inspect-ai；样本数 `--limit 5` 默认。
   - Dataset 从 E2-T01 锁的 `evals/datasets/manifests/martian_2026wXX.yaml` 读 commit hash + sample list。
   - Solver = `openbot_review_solver()` (E1-T06)。
   - Scorer = `review_overlap_scorer()`（E1-T07 接通）。
   - Run-level metadata = `collect_run_metadata("review_martian", "smoke", dataset_version=..., dataset_sha256=...)`，传 `log_run_metadata` 写到 LangSmith run。
   - Per-sample artifact 上传走 `export_artifact(...)` (E1-T04)。
2. 跑一次 `inspect eval evals/tasks/review_martian.py --limit 5`。
3. 跑 `python scripts/validate_langsmith_run.py <run_id>` 确认 metadata 校验过。
4. handoff 改为 `✅ done`，STATE.md E1-T08 从 Blocked 移到 Done，touching commit-msg "feat(E1-T08): ..."。

## Tests run

```
$ make check
# 124 passed in <=2s  (no new tests; placeholder file is import-only)
```

## Pointers for next task
- **E1-T09** · Run summary 导出脚本 —— Deps E1-T08 (blocked)，连锁 ❌ blocked。下游会写一个同型 skeleton + handoff。
- **E1-T10** · 双 LangSmith project 隔离 —— Deps E1-T02 ✅，**可做**。是 E1 stack 最后一个有产出的 task。

## Open follow-ups
- 见 unblock checklist。
