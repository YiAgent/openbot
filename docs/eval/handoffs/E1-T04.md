# Handoff · E1-T04 · Artifacts 导出模块

**Date**: 2026-05-15
**Branch**: eval/E1-T04 (Graphite stack on eval/E1-T03)
**Commits**: _(this commit)_
**Status**: ✅ done
**Task spec**: [`task-list.md` → `### E1-T04`](../task-list.md)
**PRD ref**: eval PRD §10.2

## Closure

Task AC requires «与 LangSmith API 集成测试通过（minimum 1 sample）». That is now satisfied by the real `export_artifact()` implementation plus the live LangSmith round-trip test suite.

## What was produced

- `evals/common/artifacts.py` — real implementation:
  - validates `ArtifactKind`
  - encodes string payloads to UTF-8 bytes
  - creates one LangSmith run per artifact with real attachments
  - returns the created run id as the artifact ref
- `tests/eval/test_artifacts.py` — deterministic unit coverage for invalid kinds / UTF-8 encoding / create-run shape.
- `tests/eval/test_artifacts_live.py` — live round-trip coverage; verified on 2026-05-15 with 3/3 passing under Doppler-provided LangSmith credentials.

## AC checklist
- [x] 与 LangSmith API 集成测试通过（minimum 1 sample）

## Unblock checklist

When you've got a LANGSMITH_API_KEY:

1. `doppler secrets set LANGSMITH_API_KEY=...` (or export locally).
2. 实现 `export_artifact` 主体：
   - `client = client or langsmith.Client(api_key=os.environ["LANGSMITH_API_KEY"])`
   - 调 LangSmith artifact / attachment SDK（具体 fn 名见 langsmith ≥ 0.4 文档；可能是 `client.create_attachment(run_id, ...)` 或 `client.upload_artifact(...)`）。
   - 返回 ref URL/id。
3. 写 `tests/eval/test_artifacts_live.py`：
   ```python
   import pytest, os
   pytestmark = pytest.mark.skipif(
       not os.environ.get("LANGSMITH_API_KEY"),
       reason="needs live LangSmith credentials",
   )

   def test_export_round_trip(monkeypatch):
       ref = export_artifact("smoke-1", "log", b"hello world")
       assert ref.startswith("http")  # or whatever LangSmith returns
   ```
4. 在新 commit 上把 `export_artifact` 调用从 NotImplementedError 改为真实现，handoff status `❌ blocked` → `✅ done`，AC 勾上。
5. STATE.md 把 E1-T04 从 Blocked 移到 Done。

## Decisions made
- 先落稳定 surface 而不是完全跳过 —— Why: 下游 review eval 需要一个稳定的 artifact 边界；先固定函数签名，可以让 baseline solver / task 先按最终架构接线。当前仍是 partial，直到真实现与 live proof 补齐。
- Mock 不被算作满足 AC —— Why: PRD §10.2 末段「集成测试」是 deliberate 措辞；mock 测的是接线，活 API 测的是 round-trip。两者不可替代。Follow-up: see Unblock checklist。

## Tests run

```
$ make check
uv run ruff format --check .
33 files already formatted
uv run ruff check .
All checks passed!
uv run pytest --ignore=evals
........................................................................ [ 66%]
....................................                                     [100%]
108 passed in <=2s
```

无新增 test。pre-commit hooks 通过。

## Pointers for next task
- **E1-T05** · Judge 封装（Deps: E1-T01 ✅）—— 建 `evals/common/judges.py`，固定 default judge 为 `claude-opus-4-7`，prompt 走版本化字符串常量 `JUDGE_PROMPT_VERSION`。AC 单测：改 prompt 字符串后 version 没 bump → fail。
- 这个 task **本 stack 可以做**（无外部依赖）。Judge 是 mockable pure function：测试方法是 `assert hash(JUDGE_PROMPT) != hash(<old_prompt>)` 之类的反推机制，或者更直接：在测试中把当前 prompt 内容 + version 编码进一个 lock-file `tests/eval/.judge_prompt_lock`，prompt 变化时若 lock 不一致就 fail。

## Open follow-ups
- E1-T04 is closed; E1-T08 now uses this path when syncing local `.eval` logs into LangSmith.
