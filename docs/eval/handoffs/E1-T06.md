# Handoff · E1-T06 · Review solver（真实 workflow 入口）

**Date**: 2026-05-15
**Branch**: eval/E1-T06 (Graphite stack on eval/E1-T05)
**Commits**: _(this commit)_
**Status**: ❌ blocked
**Task spec**: [`task-list.md` → `### E1-T06`](../task-list.md)
**PRD ref**: eval PRD §4.1 强制 · §6.2

## Blocker

Two preconditions not met:

1. **`openbot.workflows.review.run(...)` 不存在**：CLAUDE.md 明说 v0.1 Week 1 skeleton 阶段 `openbot/` 仅含 `webapp.py` / `config.py` / `events.py` / `adapters/`；workflow 层（review / fix / triage / chat）落地是 v0.1 Week 2+ 的事。AC 要求「调到真实 workflow（不 mock）」，没法满足。
2. **E1-T04 artifact export 仍 ❌ blocked**：solver 输出归一化后需要把 raw patch / diff 上传到 LangSmith artifact（PRD §10.2 + §6.2 责任边界），调的就是 E1-T04 的 `export_artifact()`。

## What was produced (partial)

- `evals/solvers/openbot_review.py` — `openbot_review_solver()` raises `NotImplementedError`，body 里点名两个 blocker 与对应 handoff。下游 import 不炸，但调用炸。
- 没有单测：AC 要求集成测试（活 workflow），mock 不算。

## AC checklist
- [ ] 集成测试用 fake PR diff，确认调到真实 workflow（不 mock） — **blocked: `openbot.workflows.review` 不存在**
- [ ] 输出归一化为 `{file, line, body, severity}` — **blocked: 同上**

## Unblock checklist

1. **`openbot.workflows.review.run` 上线**（main PRD §4.1 强制 workflow，预期 v0.1 Week 2 落地）。
2. E1-T04 unblock（拿到 LANGSMITH_API_KEY；见 handoffs/E1-T04.md）。
3. 实现 `openbot_review_solver()`：
   - 用 inspect-ai `@solver` 装饰器（`from inspect_ai.solver import solver, TaskState`）。
   - 从 sample 取 PR diff（input artifact）。
   - 调 `openbot.workflows.review.run(diff=...)`。
   - 把 raw output 上传 `evals.common.artifacts.export_artifact(sample_id, "raw_output", ...)`，把 ref 写进 `sample.metadata.output_artifact_refs`。
   - 归一化输出为 `[{file, line, body, severity}]`，写入 `TaskState.output`。
4. 写 `tests/eval/test_openbot_review_solver_live.py`（活 workflow + 活 LangSmith；用 `pytest.mark.skipif` 守门 missing env）。
5. handoff 改为 `✅ done`，STATE.md 把 E1-T06 从 Blocked 移到 Done。

## Decisions made
- 落 skeleton 而非空文件 —— Why: 下游 E1-T08 Martian smoke task 必须 `from evals.solvers.openbot_review import openbot_review_solver`，否则 Python import 阶段就炸 collection。NotImplementedError + 指针文档化了 stub 的意图。Follow-up: 见 unblock checklist。
- 输出 schema `{file, line, body, severity}` 写进 docstring 而不是落 `TypedDict` —— Why: 等 Inspect AI 真接通时，可能会用 `Score` / `ScoreResult` / 它自家的结构；提前 TypedDict 容易绑死与 inspect-ai 版本不兼容的形状。Follow-up: unblock 时按 inspect-ai 实际 API 落 TypedDict 或 dataclass。

## Tests run

```
$ make check
uv run ruff format --check .
36 files already formatted
uv run ruff check .
All checks passed!
uv run pytest --ignore=evals
116 passed in <=2s
```

无新增 test。

## Pointers for next task
- **E1-T07** · Review overlap scorer（Deps: E1-T05 ✅） —— **本 stack 可以做**：scorer 用 `Judge` 实例做依赖注入，测试 mock judge 调用，无外部依赖。AC 三种 case：完全 match / 部分 match / 完全 miss。
- E1-T07 完成后 E1-T08（Martian smoke）依赖 E1-T06（blocked）+ E1-T07（done）+ E0-T04，仍 blocked。

## Open follow-ups
- E1-T06 unblock 两阶段：先 main PRD `openbot.workflows.review` 落地，再 LANGSMITH_API_KEY 拿到 → 然后才能签字。Owner: 主 PRD workflow 任务（非 eval 范畴）+ creds provisioning。
