# Handoff · E1-T01 · 加 `inspect-ai` + `inspect-evals` 依赖

**Date**: 2026-05-15
**Branch**: eval/E1-T01 (Graphite stack on eval/E0)
**Commits**: _(this commit)_
**Status**: ✅ done
**Task spec**: [`task-list.md` → `### E1-T01`](../task-list.md)
**PRD ref**: eval PRD §17 #1

## What was produced
- `pyproject.toml` — `dependencies` 新增 `inspect-ai>=0.3.220` 与 `inspect-evals>=0.12.0`（uv add 自动选取的最低稳定线）。
- `uv.lock` — 解析后锁定的依赖图（含 anyio / typer / tiktoken / textual / shortuuid / wrapt 等 inspect 间接依赖）。

## AC checklist
- [x] `uv run inspect --version` 输出版本号 → `0.3.220`

## Decisions made
- 把 inspect-ai 与 inspect-evals 都放入 `[project] dependencies` 而非 `[project.optional-dependencies.eval]` —— Why: Makefile / pre-push 都用 `--ignore=evals`，evals 代码不进运行时；但 `evals/common/*` 与 `evals/scorers/*` 之后会被 `tests/eval/` 引用（E1-T07 起），不能因 optional extra 漏装。PRD §17 也把 inspect 与 langsmith 列为「核心栈」。Follow-up: 如未来想给纯线上服务镜像剥离 eval 依赖，再拆 extras。
- CI 装包验证留到下一个 push 触发：本 task 只在本地验过 `uv sync`，CI 上的「装包成功」由 push 后的 GitHub Actions 跑出（`ci.yml` 的 lint/test job 都跑 `uv sync --all-extras --dev`）。Follow-up: 看到首个 PR push 后的绿勾即视为 CI 装包通过。

## Tests run

```
$ uv add inspect-ai inspect-evals
…
 + inspect-ai==0.3.220
 + inspect-evals==0.12.0
 (+ 数十个间接依赖)
```

```
$ uv run inspect --version
0.3.220
```

```
$ make check
uv run ruff format --check .
27 files already formatted
uv run ruff check .
All checks passed!
uv run pytest --ignore=evals
............................................................             [100%]
60 passed in 0.88s
```

webhook 503 测试已在前置 chore commit（2d09b7b）修复；本 task 测试基线干净，无新失败。

## Pointers for next task
- **E1-T02** · LangSmith env 与 client 封装（Deps: E1-T01 ✅, E0-T05 ✅）—— 建 `evals/common/langsmith.py`，封装 `init_client()` / `log_run_metadata()` / `log_sample()`。
- `inspect-ai` 已经把 `langsmith` 作为间接依赖装好（看 `uv.lock`）；E1-T02 不需要再 `uv add langsmith`，直接 `import langsmith`。
- 单测复用 `scripts/validate_langsmith_run.py` 里 `REQUIRED_RUN_METADATA_FIELDS` 常量做断言（E0-T04 已经把它定义为 single source of truth）。
- `.env.example` 需要补 `LANGSMITH_API_KEY` / `LANGSMITH_PROJECT_INTERNAL` / `LANGSMITH_PROJECT_PUBLIC` 三行。
- 注意：本 task 的 AC「CI 装包成功」由 push 后的 GHA 验证；E1-T02 不依赖它。

## Open follow-ups
- 无 milestone-blocking。
