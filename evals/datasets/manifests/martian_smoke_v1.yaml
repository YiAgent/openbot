# evals/datasets/manifests/martian_smoke_v1.yaml
#
# v0.1 SMOKE dataset — NOT the real Martian benchmark.
#
# Purpose: prove the deepagents review pipeline end-to-end (solver + scorer
# + judge + LangSmith logging + artifact upload). Real upstream Martian
# benchmark lock lands in E2-T01 with a `martian_<YYYYwWW>_v1.yaml` manifest.
#
name: martian_smoke_v1
version: 1
created_at: "2026-05-15"
source: "hand-authored synthetic PR diffs covering: timing-attack, SQL-injection, resource-leak, TOCTOU, clean-diff"
sampling_rule: "hand-picked to span PRD §10.3 severity tiers and one clean-diff false-positive guard"
sample_count: 5
sha256: "31ad7d8f211b290877b946cf6bfd01ae0e38b8607d1a5bdce478f1178d4122aa"
golden_generation: "human-authored"
public: false  # internal project until v0.1 baseline is signed off (PRD §13.2 fail-safe)
license: "internal-only"
related_suites: [review_martian]
deprecated: false
notes: |
  Synthetic. Each sample is a tiny PR diff plus a golden findings list
  (`[{file, line, body, severity}]`). One sample (smoke-005) is intentionally
  clean — guards against false-positive review behavior.

  When E2-T01 lands the real Martian dataset, this manifest can stay around
  as the cheap smoke target, or be marked `deprecated: true`.
